library(Seurat)
library(patchwork)
library(dplyr)

setwd("c:/Users/xjmik/Downloads/data.expression/data/expression/CD4/integration/")
a<-readRDS("int.CD4.S35.sce.merged.rds")
b<-readRDS("int.CD4.S35.meta.tb.rds")
setwd("c:/Users/xjmik/Downloads/data.expression/data/expression/CD8/integration/")
c<-readRDS("int.CD8.S35.sce.merged.rds")
d<-readRDS("int.CD8.S35.meta.tb.rds")
count_CD4<-a@assays[["data"]]@listData[["exprs"]]
count_CD4<-as.data.frame(count_CD4)
count_CD8<-c@assays[["data"]]@listData[["exprs"]]
count_CD8<-as.data.frame(count_CD8)
count<-cbind(count_CD4,count_CD8)
metadata<-rbind(b,d)
remove(b,d)
remove(count_CD4,count_CD8)
metadata_CD4<-as.data.frame(a@colData@listData)
metadata_CD8<-as.data.frame(c@colData@listData)
metadata_new<-rbind(metadata_CD4,metadata_CD8)
remove(metadata_CD4,metadata_CD8,a,c)
